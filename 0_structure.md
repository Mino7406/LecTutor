# 손글씨 순서도 텍스트 변환

## 1. 입력 및 초기 처리 (Phase 1)

### 1.1 입력 및 로컬 처리
* **입력 (⓪)**: 사용자가 PDF를 입력
* **로컬 처리**: 파일 스캔 + 전처리
    * ↓
    * **헤더 추출 (①)**: 페이지별 헤더 식별

### 1.2 AI 구조화
* **입력**: `① (추출된 헤더)`
* **처리**: 헤더 범위 추출
* **출력**: `② (챕터 범위 JSON)`

### 1.3 출력 (UI)
* 사용자가 `② (출력 결과)` 확인
* *Note: 여기서 확정된 범위가 이후 루프의 기준이 됨*

---

## 2. 반복 실행 루프 (Phase 2)
> **Loop 진입**: 사용자 확인 후 시작

### 2.1 데이터 준비
* **로드 (③)**: 용어집 + 요약 로드 (초기엔 빈값)
    * ↓
* **데이터 가공**: `② (챕터 범위)`를 참고하여 `⓪ (PDF 원본)`의 사본을 챕터 단위로 자름
* **생성 (④)**: 챕터당 원문 생성

### 2.2 번역 요청
* **Action**: AI에게 `④ (원문)` 번역 요청

---

## 3. 결과 처리 및 조건 분기 (Update & Condition)

### 3.1 결과 저장 및 갱신
* **결과 저장 (⑤)**: 번역본에 번역 내용 추가 (최종 파일에 Append)
* **메타데이터 갱신 (⑥)**:
    * 요약에 다음 내용 업데이트
    * 마지막 문장, 맥락, 용어집 업데이트

### 3.2 조건 확인
* **< 챕터가 끝인가? >**
    * **`False` (아직 남음)**: 다시 **[Phase 2 - ③번]** (용어집+요약 로드) 단계로 돌아가서 **Loop**
    * **`True` (다 끝남)**: → **완성본을 출력** (End)